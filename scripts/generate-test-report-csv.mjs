/**
 * Generate comprehensive test report for "Tiệm Bách Hóa Hai Tụi Mình"
 * Run: node scripts/generate-test-report-csv.mjs
 */

import { writeFileSync } from 'fs';

// ─── Utility: write XLSX ───────────────────────────────────────────────────────
// Pure-JS minimal XLSX builder (no dependencies needed)
function buildXLSX(sheets) {
  // We'll output a well-structured CSV-like format but actually use a proper
  // XLSX-compatible approach via SheetJS-style XML.
  // Since we can use the already-installed xlsx or exceljs via node_modules...
  // Let's check if exceljs is available via the project's node_modules
  return sheets;
}

// ─── Test Data Definition ──────────────────────────────────────────────────────

const STATUS = {
  PASS: 'PASS',
  FAIL: 'FAIL',
  PENDING: 'CHƯA KIỂM THỬ',
  BLOCKED: 'BLOCKED',
  NA: 'N/A',
};

const PRIORITY = { HIGH: 'CAO', MEDIUM: 'TRUNG BÌNH', LOW: 'THẤP' };
const TYPE = {
  UNIT: 'Unit Test',
  INTEGRATION: 'Integration Test',
  E2E: 'E2E Test',
  SECURITY: 'Security Test',
  PERFORMANCE: 'Performance Test',
  UI_UX: 'UI/UX Test',
  REGRESSION: 'Regression Test',
};

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 1: UNIT TESTS - Cart Store (Zustand)
// ─────────────────────────────────────────────────────────────────────────────
const unitTestsCart = [
  {
    id: 'UT-CART-001',
    module: 'Cart Store (Zustand)',
    feature: 'addItem',
    testCase: 'Thêm sản phẩm hợp lệ vào giỏ hàng trống',
    precondition: 'Cart store rỗng (items = [])',
    steps: '1. Gọi addItem(product) với stock > 0\n2. Kiểm tra items',
    expectedResult: 'items có 1 phần tử, quantity = 1, totalItems = 1, totalPrice = giá sản phẩm',
    actualResult: 'PASS - Test tồn tại trong store.test.ts (it: "adds an in-stock product with quantity 1")',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'Đã có test case, kết quả: PASS',
  },
  {
    id: 'UT-CART-002',
    module: 'Cart Store (Zustand)',
    feature: 'addItem',
    testCase: 'Không thêm sản phẩm có stock = 0',
    precondition: 'Cart store rỗng',
    steps: '1. Gọi addItem(product) với stock = 0\n2. Kiểm tra items',
    expectedResult: 'items vẫn rỗng []',
    actualResult: 'PASS - Test tồn tại (it: "does not add products that have no stock")',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'Đã có test case, kết quả: PASS',
  },
  {
    id: 'UT-CART-003',
    module: 'Cart Store (Zustand)',
    feature: 'addItem',
    testCase: 'Không thêm sản phẩm có stock âm (-5)',
    precondition: 'Cart store rỗng',
    steps: '1. Gọi addItem(product) với stock = -5',
    expectedResult: 'items vẫn rỗng',
    actualResult: 'PASS - Covered trong "does not add products that have no stock"',
    status: STATUS.PASS,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'Đã có test case',
  },
  {
    id: 'UT-CART-004',
    module: 'Cart Store (Zustand)',
    feature: 'addItem',
    testCase: 'Số lượng bị giới hạn tại stock khi thêm nhiều lần',
    precondition: 'Product có stock = 2',
    steps: '1. addItem(product) x3 lần\n2. Kiểm tra quantity',
    expectedResult: 'quantity = 2 (bị cap tại stock)',
    actualResult: 'PASS - Test tồn tại (it: "caps repeated add-to-cart")',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'Logic clampQuantity hoạt động đúng',
  },
  {
    id: 'UT-CART-005',
    module: 'Cart Store (Zustand)',
    feature: 'updateQuantity',
    testCase: 'Clamp số lượng vượt quá stock',
    precondition: 'Có 1 item với stock = 3',
    steps: '1. updateQuantity(id, 99)',
    expectedResult: 'quantity = 3 (bị cap tại stock)',
    actualResult: 'PASS - Test tồn tại',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'clampQuantity hoạt động đúng',
  },
  {
    id: 'UT-CART-006',
    module: 'Cart Store (Zustand)',
    feature: 'updateQuantity',
    testCase: 'Clamp số lượng về 1 khi nhập 0',
    precondition: 'Có 1 item',
    steps: '1. updateQuantity(id, 0)',
    expectedResult: 'quantity = 1 (minimum là 1)',
    actualResult: 'PASS - Test tồn tại',
    status: STATUS.PASS,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'Clamp logic đúng',
  },
  {
    id: 'UT-CART-007',
    module: 'Cart Store (Zustand)',
    feature: 'updateQuantity',
    testCase: 'Xóa item khỏi cart khi stock sản phẩm = 0 và cố update',
    precondition: 'Item với stock = 0 bị force vào state',
    steps: '1. updateQuantity(id, 1)',
    expectedResult: 'Item bị xóa khỏi cart (quantity = 0)',
    actualResult: 'PASS - Test tồn tại',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'Filter sau khi update đúng',
  },
  {
    id: 'UT-CART-008',
    module: 'Cart Store (Zustand)',
    feature: 'restoreItems',
    testCase: 'Restore items không vượt quá stock',
    precondition: 'Cart rỗng',
    steps: '1. restoreItems([{stock:5, qty:2}, {stock:1, qty:9}, {stock:0, qty:1}])',
    expectedResult: '2 items được restore, qty bị cap: [2, 1], out-of-stock bị bỏ qua',
    actualResult: 'PASS - Test tồn tại',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'Đúng spec: totalItems=3, totalPrice=290000',
  },
  {
    id: 'UT-CART-009',
    module: 'Cart Store (Zustand)',
    feature: 'restoreItems',
    testCase: 'Merge restored items với cart hiện có',
    precondition: 'Cart có 1 item (qty=1), restore cùng item (qty=2)',
    steps: '1. addItem(product) -> 2. restoreItems([same product, qty:2])',
    expectedResult: 'Vẫn 1 item, quantity = 3 (merge và cap tại stock=3)',
    actualResult: 'PASS - Test tồn tại',
    status: STATUS.PASS,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'Đúng spec',
  },
  {
    id: 'UT-CART-010',
    module: 'Cart Store (Zustand)',
    feature: 'removeItem',
    testCase: 'Xóa sản phẩm khỏi giỏ hàng',
    precondition: 'Cart có 2 items',
    steps: '1. removeItem(productId)\n2. Kiểm tra items',
    expectedResult: 'Chỉ còn 1 item',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - removeItem chưa được test trong store.test.ts',
  },
  {
    id: 'UT-CART-011',
    module: 'Cart Store (Zustand)',
    feature: 'clearCart',
    testCase: 'Xóa toàn bộ giỏ hàng',
    precondition: 'Cart có nhiều items',
    steps: '1. clearCart()',
    expectedResult: 'items = []',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - clearCart chưa được test',
  },
  {
    id: 'UT-CART-012',
    module: 'Cart Store (Zustand)',
    feature: 'Persistence (localStorage)',
    testCase: 'Cart được persist vào localStorage',
    precondition: 'Cart có items',
    steps: '1. addItem(product)\n2. Kiểm tra localStorage["cart-storage"]',
    expectedResult: 'localStorage có dữ liệu cart',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - persist chưa được test',
  },
  {
    id: 'UT-CART-013',
    module: 'Cart Store (Zustand)',
    feature: 'Persistence Hydration',
    testCase: 'Cart được hydrate đúng từ localStorage với data dirty (quantity > stock)',
    precondition: 'localStorage có item với quantity=99, stock=5',
    steps: '1. Load lại app\n2. Kiểm tra cart state',
    expectedResult: 'quantity bị clamp về 5',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - sanitizeCartItems trong merge chưa được test riêng',
  },
  {
    id: 'UT-CART-014',
    module: 'Cart Store (Zustand)',
    feature: 'totalPrice',
    testCase: 'totalPrice tính đúng với nhiều items',
    precondition: 'Cart có items: [{price:120000, qty:2}, {price:50000, qty:3}]',
    steps: '1. Kiểm tra totalPrice()',
    expectedResult: '120000*2 + 50000*3 = 390000',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE riêng cho totalPrice với nhiều items',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 2: UNIT TESTS - Utility Functions
// ─────────────────────────────────────────────────────────────────────────────
const unitTestsUtils = [
  {
    id: 'UT-UTIL-001',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'formatCurrencyVND',
    testCase: 'Format số tiền 120000 thành chuỗi VND',
    precondition: 'N/A',
    steps: '1. formatCurrencyVND(120000)',
    expectedResult: '"120.000 ₫" hoặc định dạng VND hợp lệ',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - hàm quan trọng nhưng chưa có unit test',
  },
  {
    id: 'UT-UTIL-002',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'formatCurrencyVND',
    testCase: 'Format số 0',
    precondition: 'N/A',
    steps: '1. formatCurrencyVND(0)',
    expectedResult: '"0 ₫"',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE',
  },
  {
    id: 'UT-UTIL-003',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'formatCurrencyVND',
    testCase: 'Format số âm',
    precondition: 'N/A',
    steps: '1. formatCurrencyVND(-50000)',
    expectedResult: 'Hiển thị số âm đúng định dạng',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - edge case',
  },
  {
    id: 'UT-UTIL-004',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'formatCompactCurrencyVND',
    testCase: 'Format 1,500,000 thành "1.5 triệu"',
    precondition: 'N/A',
    steps: '1. formatCompactCurrencyVND(1500000)',
    expectedResult: '"1.5 triệu"',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE',
  },
  {
    id: 'UT-UTIL-005',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'formatCompactCurrencyVND',
    testCase: 'Format 2,000,000,000 thành "2.0 tỷ"',
    precondition: 'N/A',
    steps: '1. formatCompactCurrencyVND(2000000000)',
    expectedResult: '"2.0 tỷ"',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE',
  },
  {
    id: 'UT-UTIL-006',
    module: 'Utils (shared/lib/utils.ts)',
    feature: 'cn (classnames)',
    testCase: 'Merge classnames đúng',
    precondition: 'N/A',
    steps: '1. cn("a", undefined, "b", false && "c")',
    expectedResult: '"a b"',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.UNIT,
    notes: 'THIẾU TEST CASE - phụ thuộc vào clsx + tailwind-merge',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 3: API TESTS - Auth Endpoints
// ─────────────────────────────────────────────────────────────────────────────
const apiTestsAuth = [
  {
    id: 'API-AUTH-001',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký tài khoản với thông tin hợp lệ',
    precondition: 'Email chưa tồn tại trong DB',
    steps: 'POST /api/auth/register\nBody: {email: "test@test.com", password: "123456", name: "Test User"}',
    expectedResult: 'HTTP 201, trả về {user: {...}, token: "<jwt>"}, set cookie refreshToken',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Chưa có integration test',
  },
  {
    id: 'API-AUTH-002',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký với email đã tồn tại',
    precondition: 'Email đã có trong DB',
    steps: 'POST /api/auth/register\nBody: {email: "<existing>", ...}',
    expectedResult: 'HTTP 400, {error: "Email này đã được sử dụng"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Chưa có integration test',
  },
  {
    id: 'API-AUTH-003',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký với email không hợp lệ',
    precondition: 'N/A',
    steps: 'POST /api/auth/register\nBody: {email: "not-an-email", password: "123456", name: "Test"}',
    expectedResult: 'HTTP 400, {error: "Email không hợp lệ"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Validation Zod, chưa có integration test',
  },
  {
    id: 'API-AUTH-004',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký với mật khẩu < 6 ký tự',
    precondition: 'N/A',
    steps: 'POST /api/auth/register\nBody: {email: "test@test.com", password: "12345", name: "Test"}',
    expectedResult: 'HTTP 400, {error: "Mật khẩu phải có ít nhất 6 ký tự"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Validation Zod',
  },
  {
    id: 'API-AUTH-005',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký với tên < 2 ký tự',
    precondition: 'N/A',
    steps: 'POST /api/auth/register\nBody: {email: "test@test.com", password: "123456", name: "A"}',
    expectedResult: 'HTTP 400, thông báo lỗi validation',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: 'Validation Zod',
  },
  {
    id: 'API-AUTH-006',
    module: 'Auth API',
    feature: 'POST /api/auth/register',
    testCase: 'Đăng ký tạo cart và wishlist mặc định',
    precondition: 'Email chưa tồn tại',
    steps: 'POST /api/auth/register với data hợp lệ\nKiểm tra DB sau đăng ký',
    expectedResult: 'User được tạo kèm Cart và Wishlist rỗng trong DB',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'BUG TIỀM ẨN: Nếu DB fail sau khi user được tạo nhưng trước khi cart được tạo => không rollback',
  },
  {
    id: 'API-AUTH-007',
    module: 'Auth API',
    feature: 'POST /api/auth/login',
    testCase: 'Đăng nhập với thông tin đúng',
    precondition: 'User đã tồn tại và active',
    steps: 'POST /api/auth/login\nBody: {email: "test@test.com", password: "123456"}',
    expectedResult: 'HTTP 200, {user: {...}, token: "<jwt>"}, set refreshToken cookie',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Chưa có integration test',
  },
  {
    id: 'API-AUTH-008',
    module: 'Auth API',
    feature: 'POST /api/auth/login',
    testCase: 'Đăng nhập với email không tồn tại',
    precondition: 'Email chưa đăng ký',
    steps: 'POST /api/auth/login\nBody: {email: "none@none.com", password: "123456"}',
    expectedResult: 'HTTP 401, {error: "Email hoặc mật khẩu không chính xác"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Thông báo lỗi không tiết lộ email có tồn tại không - ĐÚNG',
  },
  {
    id: 'API-AUTH-009',
    module: 'Auth API',
    feature: 'POST /api/auth/login',
    testCase: 'Đăng nhập với sai mật khẩu',
    precondition: 'Email tồn tại',
    steps: 'POST /api/auth/login\nBody: {email: "test@test.com", password: "wrongpass"}',
    expectedResult: 'HTTP 401, {error: "Email hoặc mật khẩu không chính xác"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-010',
    module: 'Auth API',
    feature: 'POST /api/auth/login',
    testCase: 'Đăng nhập tài khoản bị khóa (isActive=false)',
    precondition: 'User có isActive=false',
    steps: 'POST /api/auth/login với đúng credentials',
    expectedResult: 'HTTP 403, {error: "Tài khoản của bạn đã bị khóa..."}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-011',
    module: 'Auth API',
    feature: 'GET /api/auth/me',
    testCase: 'Lấy thông tin user với token hợp lệ',
    precondition: 'Token JWT còn hạn',
    steps: 'GET /api/auth/me\nHeader: Authorization: Bearer <valid-token>',
    expectedResult: 'HTTP 200, trả về thông tin user (không có deletedAt, password)',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'BUG TIỀM ẨN: deletedAt vẫn được select trong authUserSelect, chỉ bị xóa qua serializeAuthUser',
  },
  {
    id: 'API-AUTH-012',
    module: 'Auth API',
    feature: 'GET /api/auth/me',
    testCase: 'Lấy thông tin user không có token',
    precondition: 'Không có Authorization header',
    steps: 'GET /api/auth/me (không header)',
    expectedResult: 'HTTP 401, {error: "Unauthorized"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-013',
    module: 'Auth API',
    feature: 'GET /api/auth/me',
    testCase: 'Lấy thông tin user với token hết hạn',
    precondition: 'Token JWT đã expire',
    steps: 'GET /api/auth/me\nHeader: Authorization: Bearer <expired-token>',
    expectedResult: 'HTTP 401, {error: "Invalid token"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-014',
    module: 'Auth API',
    feature: 'POST /api/auth/refresh',
    testCase: 'Refresh token hợp lệ',
    precondition: 'Cookie refreshToken hợp lệ',
    steps: 'POST /api/auth/refresh với cookie refreshToken',
    expectedResult: 'HTTP 200, {token: "<new-access-token>"}, cookie refreshToken mới được set',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Token rotation đúng - xóa token cũ, tạo token mới',
  },
  {
    id: 'API-AUTH-015',
    module: 'Auth API',
    feature: 'POST /api/auth/refresh',
    testCase: 'Refresh token hết hạn',
    precondition: 'Cookie refreshToken đã hết hạn',
    steps: 'POST /api/auth/refresh với expired cookie',
    expectedResult: 'HTTP 401, cookie bị clear',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-016',
    module: 'Auth API',
    feature: 'POST /api/auth/logout',
    testCase: 'Đăng xuất với token hợp lệ',
    precondition: 'User đã đăng nhập',
    steps: 'POST /api/auth/logout với cookie + Authorization header',
    expectedResult: 'HTTP 200, {success: true}, refreshToken cookie bị clear, refresh token xóa khỏi DB',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-AUTH-017',
    module: 'Auth API',
    feature: 'Rate Limiting',
    testCase: 'Gửi hơn 10 requests đăng nhập trong 15 phút (production)',
    precondition: 'NODE_ENV=production',
    steps: 'POST /api/auth/login x11 lần',
    expectedResult: 'HTTP 429, thông báo rate limit',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'Rate limiter: 10 req/15min (prod), 200 req/1min (dev)',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 4: API TESTS - Product Endpoints
// ─────────────────────────────────────────────────────────────────────────────
const apiTestsProduct = [
  {
    id: 'API-PROD-001',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Lấy danh sách sản phẩm mặc định (trang 1, limit 12)',
    precondition: 'DB có sản phẩm',
    steps: 'GET /api/products',
    expectedResult: 'HTTP 200, {success: true, data: [...], meta: {total, page:1, limit:12, totalPages}}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-002',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Tìm kiếm theo từ khóa',
    precondition: 'DB có sản phẩm',
    steps: 'GET /api/products?query=tai+nghe',
    expectedResult: 'Trả về sản phẩm có tên/mô tả/SKU chứa "tai nghe" (case-insensitive)',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-003',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Lọc theo danh mục',
    precondition: 'DB có sản phẩm với category',
    steps: 'GET /api/products?category=cong-nghe',
    expectedResult: 'Chỉ trả về sản phẩm thuộc category slug "cong-nghe"',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-004',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Lọc theo khoảng giá',
    precondition: 'DB có sản phẩm nhiều mức giá',
    steps: 'GET /api/products?minPrice=100000&maxPrice=500000',
    expectedResult: 'Chỉ trả về sản phẩm có giá trong khoảng [100000, 500000]',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-005',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Sắp xếp theo giá tăng dần',
    precondition: 'DB có nhiều sản phẩm',
    steps: 'GET /api/products?sortBy=price-asc',
    expectedResult: 'Sản phẩm được sắp xếp giá tăng dần',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-006',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Sắp xếp theo giá giảm dần',
    precondition: 'DB có nhiều sản phẩm',
    steps: 'GET /api/products?sortBy=price-desc',
    expectedResult: 'Sản phẩm được sắp xếp giá giảm dần',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-007',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Sắp xếp theo phổ biến (soldCount)',
    precondition: 'DB có nhiều sản phẩm',
    steps: 'GET /api/products?sortBy=popular',
    expectedResult: 'Sản phẩm được sắp xếp theo soldCount giảm dần',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-008',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Phân trang: page=2, limit=5',
    precondition: 'DB có hơn 5 sản phẩm',
    steps: 'GET /api/products?page=2&limit=5',
    expectedResult: 'Trả về sản phẩm từ index 5-9, meta.page=2, meta.limit=5',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-009',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Limit bị giới hạn tối đa 48',
    precondition: 'N/A',
    steps: 'GET /api/products?limit=1000',
    expectedResult: 'Chỉ trả về tối đa 48 sản phẩm',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: 'parsedLimit = Math.min(48, ...)',
  },
  {
    id: 'API-PROD-010',
    module: 'Product API',
    feature: 'GET /api/products',
    testCase: 'Lọc theo minRating - áp dụng sau khi lấy DB (BUG LOGIC)',
    precondition: 'DB có sản phẩm',
    steps: 'GET /api/products?minRating=4.7',
    expectedResult: 'Chỉ sản phẩm có rating >= 4.7 (nhưng rating là hardcoded theo category)',
    actualResult: 'BUG: minRating filter áp dụng SAU khi query DB, không tính vào total count => pagination bị sai',
    status: STATUS.FAIL,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'BUG: total count không phản ánh đúng số sản phẩm sau khi filter rating. Rating là giá trị hardcoded theo category slug không phải từ DB.',
  },
  {
    id: 'API-PROD-011',
    module: 'Product API',
    feature: 'GET /api/products/:id',
    testCase: 'Lấy chi tiết sản phẩm với ID hợp lệ',
    precondition: 'Sản phẩm tồn tại và active',
    steps: 'GET /api/products/<valid-id>',
    expectedResult: 'HTTP 200, trả về đầy đủ thông tin sản phẩm',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-012',
    module: 'Product API',
    feature: 'GET /api/products/:id',
    testCase: 'Lấy sản phẩm không tồn tại',
    precondition: 'ID không có trong DB',
    steps: 'GET /api/products/nonexistent-id',
    expectedResult: 'HTTP 404, {error: "Sản phẩm không tồn tại..."}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-013',
    module: 'Product API',
    feature: 'GET /api/products/:id',
    testCase: 'Lấy sản phẩm đã bị soft-delete (deletedAt != null)',
    precondition: 'Sản phẩm có deletedAt được set',
    steps: 'GET /api/products/<deleted-id>',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-014',
    module: 'Product API',
    feature: 'GET /api/products/brands',
    testCase: 'Lấy danh sách thương hiệu',
    precondition: 'DB có sản phẩm có brand',
    steps: 'GET /api/products/brands',
    expectedResult: 'HTTP 200, array [{name, count}] sắp xếp theo alphabet',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-PROD-015',
    module: 'Product API',
    feature: 'GET /api/products/facets',
    testCase: 'Lấy facets cho bộ lọc',
    precondition: 'DB có dữ liệu',
    steps: 'GET /api/products/facets',
    expectedResult: 'HTTP 200, {categories: [...], brands: [...], total: n}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 5: API TESTS - Cart & Order Endpoints
// ─────────────────────────────────────────────────────────────────────────────
const apiTestsCartOrder = [
  {
    id: 'API-CART-001',
    module: 'Cart API',
    feature: 'GET /api/cart',
    testCase: 'Lấy giỏ hàng của user đã đăng nhập',
    precondition: 'User đã đăng nhập, có cart',
    steps: 'GET /api/cart với Authorization header',
    expectedResult: 'HTTP 200, trả về cart với items',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-002',
    module: 'Cart API',
    feature: 'GET /api/cart',
    testCase: 'Lấy giỏ hàng khi không có token',
    precondition: 'Không có token',
    steps: 'GET /api/cart (không Authorization)',
    expectedResult: 'HTTP 401',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-003',
    module: 'Cart API',
    feature: 'POST /api/cart/items',
    testCase: 'Thêm sản phẩm hợp lệ vào cart',
    precondition: 'User đã đăng nhập, sản phẩm tồn tại và có stock',
    steps: 'POST /api/cart/items\nBody: {productId: "<uuid>", quantity: 2}',
    expectedResult: 'HTTP 200, {success: true, message: "Đã thêm vào giỏ hàng", data: {...}}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-004',
    module: 'Cart API',
    feature: 'POST /api/cart/items',
    testCase: 'Thêm sản phẩm vượt quá stock',
    precondition: 'Sản phẩm có stock = 2, thêm quantity = 5',
    steps: 'POST /api/cart/items\nBody: {productId: "<uuid>", quantity: 5}',
    expectedResult: 'HTTP 400, {error: "Số lượng tồn kho không đủ"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'BUG TIỀM ẨN: Check stock tại thời điểm API call, không tính lượng đã trong giỏ',
  },
  {
    id: 'API-CART-005',
    module: 'Cart API',
    feature: 'POST /api/cart/items',
    testCase: 'Thêm sản phẩm không hợp lệ (UUID sai định dạng)',
    precondition: 'N/A',
    steps: 'POST /api/cart/items\nBody: {productId: "not-uuid", quantity: 1}',
    expectedResult: 'HTTP 400, lỗi validation',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: 'cartItemSchema dùng z.string().uuid()',
  },
  {
    id: 'API-CART-006',
    module: 'Cart API',
    feature: 'POST /api/cart/items',
    testCase: 'Thêm sản phẩm với quantity = 0',
    precondition: 'N/A',
    steps: 'POST /api/cart/items\nBody: {productId: "<uuid>", quantity: 0}',
    expectedResult: 'HTTP 400, lỗi validation (quantity.min(1))',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-007',
    module: 'Cart API',
    feature: 'POST /api/cart/items',
    testCase: 'Thêm sản phẩm đã có trong cart (upsert - tăng quantity)',
    precondition: 'Cart đã có productId này với quantity=1',
    steps: 'POST /api/cart/items\nBody: {productId: "<existing>", quantity: 1}',
    expectedResult: 'HTTP 200, quantity được increment lên 2',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-008',
    module: 'Cart API',
    feature: 'PATCH /api/cart/items/:itemId',
    testCase: 'Cập nhật số lượng item trong cart',
    precondition: 'Cart có item, item thuộc user',
    steps: 'PATCH /api/cart/items/<itemId>\nBody: {quantity: 3}',
    expectedResult: 'HTTP 200, quantity được cập nhật',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-009',
    module: 'Cart API',
    feature: 'PATCH /api/cart/items/:itemId',
    testCase: 'User A cố cập nhật item của User B (IDOR)',
    precondition: 'User A và User B có cart riêng',
    steps: 'User A PATCH /api/cart/items/<itemId-của-B>',
    expectedResult: 'HTTP 404, không cho phép truy cập item của user khác',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'IDOR check: cartItem.cart.userId !== req.user.id',
  },
  {
    id: 'API-CART-010',
    module: 'Cart API',
    feature: 'DELETE /api/cart/items/:itemId',
    testCase: 'Xóa item khỏi cart',
    precondition: 'Item tồn tại trong cart của user',
    steps: 'DELETE /api/cart/items/<itemId>',
    expectedResult: 'HTTP 200, item bị xóa',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-CART-011',
    module: 'Cart API',
    feature: 'DELETE /api/cart/items/:itemId',
    testCase: 'User A cố xóa item của User B (IDOR)',
    precondition: 'User A và User B có cart riêng',
    steps: 'User A DELETE /api/cart/items/<itemId-của-B>',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'IDOR check',
  },
  {
    id: 'API-ORDER-001',
    module: 'Order API',
    feature: 'POST /api/orders/checkout',
    testCase: 'Checkout thành công với COD',
    precondition: 'User đăng nhập, cart có items, đủ stock',
    steps: 'POST /api/orders/checkout\nBody: {shippingAddress: "123 ABC Street...", shippingMethod: "STANDARD", paymentMethod: "COD"}',
    expectedResult: 'HTTP 201, order được tạo, stock giảm, cart bị clear',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'COD => status=PROCESSING, stock giảm ngay',
  },
  {
    id: 'API-ORDER-002',
    module: 'Order API',
    feature: 'POST /api/orders/checkout',
    testCase: 'Checkout với cart rỗng',
    precondition: 'Cart rỗng',
    steps: 'POST /api/orders/checkout',
    expectedResult: 'HTTP 400, {error: "Giỏ hàng trống"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-ORDER-003',
    module: 'Order API',
    feature: 'POST /api/orders/checkout',
    testCase: 'Checkout với địa chỉ giao hàng < 10 ký tự',
    precondition: 'Cart có items',
    steps: 'POST /api/orders/checkout\nBody: {shippingAddress: "ngắn", ...}',
    expectedResult: 'HTTP 400, {error: "Địa chỉ giao hàng quá ngắn"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-ORDER-004',
    module: 'Order API',
    feature: 'POST /api/orders/checkout',
    testCase: 'Checkout khi 1 sản phẩm trong cart hết stock (race condition)',
    precondition: 'Cart có sản phẩm, nhưng stock bị thay đổi 0 trước khi checkout',
    steps: '1. Thêm sản phẩm vào cart\n2. Stock sản phẩm bị set về 0 bởi admin\n3. Thực hiện checkout',
    expectedResult: 'HTTP 400, thông báo sản phẩm X không đủ tồn kho',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Atomic transaction đảm bảo consistency',
  },
  {
    id: 'API-ORDER-005',
    module: 'Order API',
    feature: 'POST /api/orders/checkout',
    testCase: 'Checkout với bank transfer (không giảm stock ngay)',
    precondition: 'Cart có items, đủ stock',
    steps: 'POST /api/orders/checkout\nBody: {paymentMethod: "BANK_TRANSFER", ...}',
    expectedResult: 'HTTP 201, order.status=PENDING, stock KHÔNG giảm ngay',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'BUG TIỀM ẨN: Stock không giảm ngay với bank transfer, có thể oversell nếu nhiều user checkout cùng lúc',
  },
  {
    id: 'API-ORDER-006',
    module: 'Order API',
    feature: 'GET /api/orders',
    testCase: 'Lấy danh sách đơn hàng của user',
    precondition: 'User đã có đơn hàng',
    steps: 'GET /api/orders với Authorization header',
    expectedResult: 'HTTP 200, array đơn hàng sắp xếp theo createdAt desc',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-ORDER-007',
    module: 'Order API',
    feature: 'GET /api/orders/:id',
    testCase: 'User A lấy đơn hàng của User B (IDOR)',
    precondition: 'User A và User B có đơn hàng riêng',
    steps: 'User A GET /api/orders/<orderId-của-B>',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'IDOR check: order.userId !== req.user.id',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 6: API TESTS - User Profile
// ─────────────────────────────────────────────────────────────────────────────
const apiTestsUser = [
  {
    id: 'API-USER-001',
    module: 'User API',
    feature: 'GET /api/user/profile',
    testCase: 'Lấy profile hợp lệ',
    precondition: 'User đã đăng nhập',
    steps: 'GET /api/user/profile',
    expectedResult: 'HTTP 200, user data (không có password)',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Đúng: password bị loại trừ',
  },
  {
    id: 'API-USER-002',
    module: 'User API',
    feature: 'PATCH /api/user/profile',
    testCase: 'Cập nhật tên hợp lệ',
    precondition: 'User đã đăng nhập',
    steps: 'PATCH /api/user/profile\nBody: {name: "Nguyễn Văn A"}',
    expectedResult: 'HTTP 200, name được cập nhật',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-USER-003',
    module: 'User API',
    feature: 'PATCH /api/user/profile',
    testCase: 'Cập nhật username đã bị dùng bởi user khác',
    precondition: 'Username "existing" đã thuộc user B',
    steps: 'PATCH /api/user/profile\nBody: {username: "existing"}',
    expectedResult: 'HTTP 400, {error: "Username đã được sử dụng"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-USER-004',
    module: 'User API',
    feature: 'PATCH /api/user/profile',
    testCase: 'Cập nhật phone số có ít hơn 10 ký tự',
    precondition: 'N/A',
    steps: 'PATCH /api/user/profile\nBody: {phone: "12345"}',
    expectedResult: 'HTTP 400, lỗi validation',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-USER-005',
    module: 'User API',
    feature: 'POST /api/user/profile/password',
    testCase: 'Đổi mật khẩu đúng currentPassword',
    precondition: 'User đã đăng nhập',
    steps: 'POST /api/user/profile/password\nBody: {currentPassword: "123456", newPassword: "newpass123"}',
    expectedResult: 'HTTP 200, mật khẩu được cập nhật',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-USER-006',
    module: 'User API',
    feature: 'POST /api/user/profile/password',
    testCase: 'Đổi mật khẩu với currentPassword sai',
    precondition: 'User đã đăng nhập',
    steps: 'POST /api/user/profile/password\nBody: {currentPassword: "wrongpass", newPassword: "newpass123"}',
    expectedResult: 'HTTP 400, {error: "Mật khẩu hiện tại không đúng"}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: '',
  },
  {
    id: 'API-USER-007',
    module: 'User API',
    feature: 'POST /api/user/addresses',
    testCase: 'Thêm địa chỉ đầu tiên (tự động set isDefault=true)',
    precondition: 'User chưa có địa chỉ nào',
    steps: 'POST /api/user/addresses\nBody: {receiverName: "Test", phone: "0901234567", province: "TP.HCM", district: "Q1", ward: "P1", detail: "123 Test St"}',
    expectedResult: 'HTTP 201, address.isDefault = true',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Logic: count===0 thì set isDefault=true',
  },
  {
    id: 'API-USER-008',
    module: 'User API',
    feature: 'DELETE /api/user/addresses/:id',
    testCase: 'Xóa địa chỉ mặc định',
    precondition: 'Địa chỉ isDefault=true',
    steps: 'DELETE /api/user/addresses/<id>',
    expectedResult: 'HTTP 400, {error: "Không thể xóa địa chỉ mặc định..."}',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.INTEGRATION,
    notes: 'Đúng logic nghiệp vụ',
  },
  {
    id: 'API-USER-009',
    module: 'User API',
    feature: 'DELETE /api/user/addresses/:id',
    testCase: 'User A xóa địa chỉ của User B (IDOR)',
    precondition: 'User A và B có địa chỉ riêng',
    steps: 'User A DELETE /api/user/addresses/<id-của-B>',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'IDOR check: existing.userId !== req.user.id',
  },
  {
    id: 'API-USER-010',
    module: 'User API',
    feature: 'GET /api/user/notifications/stream',
    testCase: 'Kết nối SSE với token hợp lệ',
    precondition: 'Token hợp lệ',
    steps: 'GET /api/user/notifications/stream?token=<valid-token>',
    expectedResult: 'HTTP 200, Content-Type: text/event-stream, ping mỗi 20s',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.INTEGRATION,
    notes: '',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 7: E2E TESTS
// ─────────────────────────────────────────────────────────────────────────────
const e2eTests = [
  {
    id: 'E2E-001',
    module: 'E2E - Cart Flow',
    feature: 'Thêm sản phẩm vào giỏ hàng',
    testCase: 'Click nút "Thêm vào giỏ" và kiểm tra badge',
    precondition: 'Trang chủ load thành công, sản phẩm hiển thị',
    steps: '1. Truy cập trang chủ\n2. Mock API products\n3. Click add-to-cart button\n4. Kiểm tra toast "Đã thêm vào giỏ hàng"\n5. Kiểm tra badge số lượng trong header',
    expectedResult: 'Toast hiển thị, badge = "1"',
    actualResult: 'PASS - Test tồn tại trong tests/cart.spec.ts',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'Đã có test, cần chạy để confirm',
  },
  {
    id: 'E2E-002',
    module: 'E2E - Cart Flow',
    feature: 'Mở cart drawer và kiểm tra item',
    testCase: 'Mở drawer giỏ hàng sau khi thêm sản phẩm',
    precondition: 'Đã có sản phẩm trong giỏ',
    steps: '1. addItem\n2. Click cart-trigger\n3. Kiểm tra heading "GIỎ HÀNG"\n4. Kiểm tra tên sản phẩm',
    expectedResult: 'Drawer mở, hiển thị sản phẩm đã thêm',
    actualResult: 'PASS - Test tồn tại trong tests/cart.spec.ts',
    status: STATUS.PASS,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'Đã có test',
  },
  {
    id: 'E2E-003',
    module: 'E2E - Auth Flow',
    feature: 'Đăng ký tài khoản',
    testCase: 'User đăng ký mới thành công',
    precondition: 'App đang chạy',
    steps: '1. Truy cập /register\n2. Nhập name, email, password\n3. Submit\n4. Kiểm tra redirect và trạng thái đăng nhập',
    expectedResult: 'Redirect đến trang chủ, user được đăng nhập',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-004',
    module: 'E2E - Auth Flow',
    feature: 'Đăng nhập',
    testCase: 'User đăng nhập thành công',
    precondition: 'User đã có tài khoản',
    steps: '1. Truy cập /login\n2. Nhập email/password\n3. Submit\n4. Kiểm tra trạng thái',
    expectedResult: 'Redirect, user info hiển thị trên header',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-005',
    module: 'E2E - Product Flow',
    feature: 'Tìm kiếm sản phẩm',
    testCase: 'Tìm kiếm và xem kết quả',
    precondition: 'App đang chạy',
    steps: '1. Nhập từ khóa vào ô search\n2. Nhấn Enter\n3. Kiểm tra kết quả',
    expectedResult: 'Trang search hiển thị sản phẩm liên quan',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-006',
    module: 'E2E - Checkout Flow',
    feature: 'Hoàn tất mua hàng',
    testCase: 'Luồng checkout đầy đủ từ cart đến order thành công',
    precondition: 'User đăng nhập, có sản phẩm trong cart',
    steps: '1. Mở cart\n2. Nhấn Checkout\n3. Điền địa chỉ\n4. Chọn phương thức\n5. Xác nhận đơn hàng',
    expectedResult: 'Order được tạo, cart bị clear, hiển thị thông báo thành công',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E - luồng quan trọng nhất',
  },
  {
    id: 'E2E-007',
    module: 'E2E - Profile',
    feature: 'Cập nhật thông tin cá nhân',
    testCase: 'User cập nhật profile',
    precondition: 'User đã đăng nhập',
    steps: '1. Truy cập /profile\n2. Cập nhật tên, số điện thoại\n3. Lưu',
    expectedResult: 'Thông tin được cập nhật, toast thành công',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-008',
    module: 'E2E - Order Tracking',
    feature: 'Theo dõi đơn hàng',
    testCase: 'User xem chi tiết đơn hàng',
    precondition: 'User đã có đơn hàng',
    steps: '1. Truy cập /order-tracking\n2. Nhập mã đơn hàng\n3. Xem trạng thái',
    expectedResult: 'Hiển thị thông tin và trạng thái đơn hàng',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-009',
    module: 'E2E - Dark Mode',
    feature: 'Chuyển đổi theme',
    testCase: 'Toggle dark/light mode',
    precondition: 'Trang web đang mở',
    steps: '1. Click nút toggle theme\n2. Kiểm tra class "dark" trên html element',
    expectedResult: 'Theme chuyển đổi không bị flash',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.LOW,
    type: TYPE.E2E,
    notes: 'THIẾU TEST CASE E2E',
  },
  {
    id: 'E2E-010',
    module: 'E2E - Responsive',
    feature: 'Mobile layout',
    testCase: 'Layout trên mobile (viewport 375px)',
    precondition: 'N/A',
    steps: '1. Set viewport 375x667\n2. Kiểm tra các thành phần hiển thị đúng',
    expectedResult: 'Mobile layout hiển thị đúng, không bị overflow',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.UI_UX,
    notes: 'THIẾU TEST CASE',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 8: SECURITY TESTS
// ─────────────────────────────────────────────────────────────────────────────
const securityTests = [
  {
    id: 'SEC-001',
    module: 'Security',
    feature: 'JWT',
    testCase: 'Token không thể giải mã nếu sai JWT_SECRET',
    precondition: 'N/A',
    steps: 'Tạo token với secret khác, gửi đến API',
    expectedResult: 'HTTP 401',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: '',
  },
  {
    id: 'SEC-002',
    module: 'Security',
    feature: 'JWT',
    testCase: 'Access token có TTL 15 phút',
    precondition: 'N/A',
    steps: 'Kiểm tra exp claim của token',
    expectedResult: 'exp = iat + 900 giây',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'ACCESS_TOKEN_EXPIRY = "15m"',
  },
  {
    id: 'SEC-003',
    module: 'Security',
    feature: 'RefreshToken Cookie',
    testCase: 'Cookie refreshToken có httpOnly=true',
    precondition: 'User đăng nhập',
    steps: 'Kiểm tra Set-Cookie header sau login',
    expectedResult: 'HttpOnly flag được set',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'httpOnly: true trong code - ĐÚNG',
  },
  {
    id: 'SEC-004',
    module: 'Security',
    feature: 'RefreshToken Cookie',
    testCase: 'Cookie refreshToken có Secure flag trong production',
    precondition: 'NODE_ENV=production',
    steps: 'Kiểm tra Set-Cookie header',
    expectedResult: 'Secure flag được set',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'secure: process.env.NODE_ENV === "production" - ĐÚNG',
  },
  {
    id: 'SEC-005',
    module: 'Security',
    feature: 'RefreshToken Cookie',
    testCase: 'Cookie có SameSite=strict',
    precondition: 'N/A',
    steps: 'Kiểm tra cookie header',
    expectedResult: 'SameSite=strict',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'sameSite: "strict" - ĐÚNG nhưng có thể gây vấn đề với OAuth sau này',
  },
  {
    id: 'SEC-006',
    module: 'Security',
    feature: 'IDOR',
    testCase: 'Không thể truy cập đơn hàng của user khác',
    precondition: 'User A và B đăng nhập riêng',
    steps: 'User A GET /api/orders/<orderId-của-B>',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'Cần kiểm tra kỹ',
  },
  {
    id: 'SEC-007',
    module: 'Security',
    feature: 'IDOR',
    testCase: 'Không thể xem/sửa cart của user khác',
    precondition: 'User A và B có cart riêng',
    steps: 'User A thao tác với items của B',
    expectedResult: 'HTTP 404',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: '',
  },
  {
    id: 'SEC-008',
    module: 'Security',
    feature: 'Admin Authorization',
    testCase: 'CUSTOMER không truy cập được /api/admin',
    precondition: 'User với role CUSTOMER',
    steps: 'GET /api/admin với token của CUSTOMER',
    expectedResult: 'HTTP 403',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'authorizeAdmin middleware',
  },
  {
    id: 'SEC-009',
    module: 'Security',
    feature: 'Helmet Headers',
    testCase: 'Security headers được set đúng',
    precondition: 'Server đang chạy',
    steps: 'Kiểm tra response headers',
    expectedResult: 'X-Content-Type-Options, X-Frame-Options, etc. được set',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.MEDIUM,
    type: TYPE.SECURITY,
    notes: 'helmet() được dùng nhưng CSP bị disable',
  },
  {
    id: 'SEC-010',
    module: 'Security',
    feature: 'SQL Injection',
    testCase: 'Không thể inject SQL qua query params',
    precondition: 'N/A',
    steps: 'GET /api/products?query=\' OR 1=1--',
    expectedResult: 'Trả về kết quả bình thường hoặc rỗng, không lỗi server',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'Prisma ORM bảo vệ khỏi SQL injection - ĐÚNG',
  },
  {
    id: 'SEC-011',
    module: 'Security',
    feature: 'Password',
    testCase: 'Mật khẩu được hash bằng bcrypt (10 rounds)',
    precondition: 'User đăng ký',
    steps: 'Kiểm tra password trong DB sau khi đăng ký',
    expectedResult: 'Password là bcrypt hash ($2b$10$...)',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'bcrypt.hash(password, 10) - ĐÚNG',
  },
  {
    id: 'SEC-012',
    module: 'Security',
    feature: 'XSS',
    testCase: 'Không thể inject script qua profile fields',
    precondition: 'User đã đăng nhập',
    steps: 'PATCH /api/user/profile\nBody: {name: "<script>alert(1)</script>"}',
    expectedResult: 'Script không được executed, stored/returned as plain text',
    actualResult: STATUS.PENDING,
    status: STATUS.PENDING,
    priority: PRIORITY.HIGH,
    type: TYPE.SECURITY,
    notes: 'React escapes HTML by default - ĐÚNG, nhưng cần kiểm tra nếu có dangerouslySetInnerHTML',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 9: BUGS FOUND
// ─────────────────────────────────────────────────────────────────────────────
const bugsFound = [
  {
    id: 'BUG-001',
    severity: 'CAO',
    module: 'Product API',
    title: 'minRating filter gây sai lệch pagination',
    description: 'GET /api/products?minRating=X áp dụng filter rating SAU khi query từ DB. Do đó: total count trong meta phản ánh tổng sản phẩm trong DB (không tính filter rating), nhưng data trả về đã bị filter. Khi client dùng meta.total để tính số trang => số trang bị sai.',
    stepsToReproduce: '1. Có 50 sản phẩm trong DB (20 thuộc cong-nghe, 30 thuộc gia-dung)\n2. GET /api/products?minRating=4.7\n3. Chỉ cong-nghe có rating 4.6 > không qua filter, gia-dung có 4.5 > không qua\n4. Data = [], meta.total = 50 (sai!)',
    impact: 'Pagination bị sai, user thấy có nhiều trang nhưng thực ra data rỗng',
    recommendation: 'Đưa rating filter vào DB query hoặc tính lại total sau khi filter',
    status: 'MỚI',
  },
  {
    id: 'BUG-002',
    severity: 'TRUNG BÌNH',
    module: 'Auth API - Register',
    title: 'Không dùng transaction khi tạo user + cart + wishlist',
    description: 'Trong auth-router.ts, user được tạo với cart và wishlist nested trong cùng 1 prisma.user.create(). Nếu DB fail ở giữa (race condition, disk full...) thì user có thể được tạo không có cart/wishlist. Nhưng nghiêm trọng hơn là: response 201 được gửi TRƯỚC khi logAction và createUserNotification hoàn tất. Nếu logging fail => không có audit trail.',
    stepsToReproduce: '1. Giả lập DB chậm\n2. Đăng ký user\n3. Kill connection sau khi user.create xong nhưng trước notification',
    impact: 'Không có audit log cho đăng ký, hệ thống compliance bị ảnh hưởng',
    recommendation: 'Dùng try/catch riêng cho notification/logging. Không để user trải nghiệm bị ảnh hưởng bởi logging failure.',
    status: 'MỚI',
  },
  {
    id: 'BUG-003',
    severity: 'CAO',
    module: 'Order API - Checkout',
    title: 'Oversell risk với bank transfer payment',
    description: 'Khi paymentMethod là bank transfer (không phải COD), stock KHÔNG được giảm khi tạo order. Nhiều user có thể checkout cùng 1 sản phẩm với bank transfer => tổng quantity vượt quá stock. Stock chỉ giảm khi admin xác nhận thanh toán (logic không tồn tại trong code hiện tại).',
    stepsToReproduce: '1. Sản phẩm có stock = 1\n2. User A checkout bank transfer qty=1\n3. User B checkout bank transfer qty=1\n4. Cả 2 đều succeed\n5. Stock vẫn = 1 nhưng có 2 đơn hàng',
    impact: 'Oversell - bán nhiều hơn hàng tồn kho',
    recommendation: 'Giảm stock ngay khi checkout bất kể payment method, hoặc dùng reserved stock concept',
    status: 'MỚI',
  },
  {
    id: 'BUG-004',
    severity: 'THẤP',
    module: 'Category API',
    title: 'ensureCoreCategories chạy mỗi GET /api/categories',
    description: 'Mỗi lần GET /api/categories, hàm ensureCoreCategories() được gọi, thực hiện 3 upsert operations vào DB. Đây là wasteful khi production traffic cao.',
    stepsToReproduce: '1. Load trang chủ nhiều lần\n2. Quan sát DB queries',
    impact: 'Performance degradation khi traffic cao',
    recommendation: 'Chạy ensureCoreCategories trong startup hook, không phải trong request handler',
    status: 'MỚI',
  },
  {
    id: 'BUG-005',
    severity: 'TRUNG BÌNH',
    module: 'Product API',
    title: 'Rating là hardcoded theo category slug, không từ DB',
    description: 'CATEGORY_RATING_MAP định nghĩa hardcoded rating (4.8, 4.5, 4.6). ProductReview model tồn tại trong DB nhưng không được dùng để tính rating thực. Đây là dữ liệu giả.',
    stepsToReproduce: 'Xem source product-router.ts dòng 8-12',
    impact: 'Rating không phản ánh đánh giá thực của khách hàng. Nếu có sản phẩm ở category ngoài 3 category này, rating = 4.5 mặc định.',
    recommendation: 'Tính rating từ ProductReview table',
    status: 'MỚI',
  },
  {
    id: 'BUG-006',
    severity: 'THẤP',
    module: 'Cart API',
    title: 'Stock check không tính quantity đã trong cart khi thêm',
    description: 'Khi POST /api/cart/items với quantity=3, chỉ check product.stock >= quantity. Nhưng không tính lượng đã có trong cart của user. Ví dụ: stock=5, cart đã có qty=4, thêm qty=3 => kiểm tra 5>=3 (PASS) nhưng tổng thực tế là 7 > 5.',
    stepsToReproduce: '1. Stock=5\n2. Cart có qty=4\n3. POST /api/cart/items qty=3\n4. API cho phép => cart có qty=7 nhưng stock chỉ có 5',
    impact: 'Có thể tạo trạng thái cart không hợp lệ',
    recommendation: 'Khi thêm vào cart, fetch current cartItem quantity và check tổng',
    status: 'MỚI',
  },
  {
    id: 'BUG-007',
    severity: 'THẤP',
    module: 'Unit Test Coverage',
    title: 'Thiếu test cho removeItem, clearCart, persistence',
    description: 'store.test.ts có 5 test cases, nhưng thiếu: removeItem, clearCart, localStorage persistence, hydration từ dirty state. Coverage thấp hơn mong đợi.',
    stepsToReproduce: 'Xem store.test.ts',
    impact: 'Rủi ro regression cao hơn',
    recommendation: 'Thêm test cases cho các hàm còn lại',
    status: 'MỚI',
  },
  {
    id: 'BUG-008',
    severity: 'TRUNG BÌNH',
    module: 'Server',
    title: 'CSP (Content Security Policy) bị disable',
    description: 'Trong server.ts: contentSecurityPolicy: false trong helmet config. CSP là một lớp bảo vệ quan trọng chống XSS.',
    stepsToReproduce: 'Xem server.ts dòng 22-24',
    impact: 'Tăng risk XSS attack',
    recommendation: 'Cấu hình CSP phù hợp với Vite, không disable hoàn toàn',
    status: 'MỚI',
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// SHEET 10: TEST SUMMARY
// ─────────────────────────────────────────────────────────────────────────────
const testSummary = {
  projectName: 'Tiệm Bách Hóa Hai Tụi Mình',
  testDate: new Date().toLocaleDateString('vi-VN'),
  tester: 'Antigravity AI - Strict QA Mode',
  version: '0.0.0',
  totalTestCases: 0, // will compute
  passed: 0,
  failed: 0,
  pending: 0,
  blocked: 0,
  passRate: '0%',
  coverageAssessment: 'THẤP - Chỉ có 7 unit tests và 2 E2E tests tồn tại. Thiếu hoàn toàn integration tests cho API.',
  criticalBugs: 3,
  mediumBugs: 3,
  lowBugs: 2,
  overallQuality: 'CẦN CẢI THIỆN',
  recommendation: [
    '1. Ưu tiên fix BUG-003 (oversell) ngay lập tức',
    '2. Fix BUG-001 (pagination sai) vì ảnh hưởng UX trực tiếp',
    '3. Thêm integration tests cho tất cả API endpoints',
    '4. Thêm unit tests cho removeItem, clearCart trong cart store',
    '5. Cấu hình CSP đúng thay vì disable',
    '6. Cân nhắc tính rating từ DB thay vì hardcode',
  ].join('\n'),
};

// Compute totals
const allTests = [
  ...unitTestsCart,
  ...unitTestsUtils,
  ...apiTestsAuth,
  ...apiTestsProduct,
  ...apiTestsCartOrder,
  ...apiTestsUser,
  ...e2eTests,
  ...securityTests,
];

testSummary.totalTestCases = allTests.length;
testSummary.passed = allTests.filter(t => t.status === STATUS.PASS).length;
testSummary.failed = allTests.filter(t => t.status === STATUS.FAIL).length;
testSummary.pending = allTests.filter(t => t.status === STATUS.PENDING).length;
testSummary.passRate = `${((testSummary.passed / testSummary.totalTestCases) * 100).toFixed(1)}%`;

// ─────────────────────────────────────────────────────────────────────────────
// OUTPUT: Generate CSV files (Excel-compatible)
// ─────────────────────────────────────────────────────────────────────────────
function escapeCsv(val) {
  if (val === null || val === undefined) return '';
  const str = String(val).replace(/"/g, '""').replace(/\r\n|\r|\n/g, ' | ');
  return `"${str}"`;
}

function toCsv(headers, rows) {
  const headerLine = headers.map(escapeCsv).join(',');
  const rowLines = rows.map(row => headers.map(h => escapeCsv(row[h] ?? '')).join(','));
  return [headerLine, ...rowLines].join('\r\n');
}

// Generate individual CSVs for each sheet
const testCaseHeaders = ['id','module','feature','testCase','precondition','steps','expectedResult','actualResult','status','priority','type','notes'];
const bugHeaders = ['id','severity','module','title','description','stepsToReproduce','impact','recommendation','status'];

const csvFiles = {
  '01_UnitTests_CartStore': toCsv(testCaseHeaders, unitTestsCart),
  '02_UnitTests_Utils': toCsv(testCaseHeaders, unitTestsUtils),
  '03_APITests_Auth': toCsv(testCaseHeaders, apiTestsAuth),
  '04_APITests_Product': toCsv(testCaseHeaders, apiTestsProduct),
  '05_APITests_CartOrder': toCsv(testCaseHeaders, apiTestsCartOrder),
  '06_APITests_User': toCsv(testCaseHeaders, apiTestsUser),
  '07_E2E_Tests': toCsv(testCaseHeaders, e2eTests),
  '08_Security_Tests': toCsv(testCaseHeaders, securityTests),
  '09_Bugs_Found': toCsv(bugHeaders, bugsFound),
};

// Generate Summary CSV
const summaryCsv = [
  '"Trường","Giá trị"',
  `"Tên dự án","${testSummary.projectName}"`,
  `"Ngày kiểm thử","${testSummary.testDate}"`,
  `"Tester","${testSummary.tester}"`,
  `"Phiên bản","${testSummary.version}"`,
  `"Tổng test cases","${testSummary.totalTestCases}"`,
  `"PASS","${testSummary.passed}"`,
  `"FAIL","${testSummary.failed}"`,
  `"CHƯA KIỂM THỬ","${testSummary.pending}"`,
  `"Pass Rate","${testSummary.passRate}"`,
  `"Đánh giá độ phủ","${testSummary.coverageAssessment}"`,
  `"Bugs nghiêm trọng cao","${testSummary.criticalBugs}"`,
  `"Bugs nghiêm trọng trung bình","${testSummary.mediumBugs}"`,
  `"Bugs nghiêm trọng thấp","${testSummary.lowBugs}"`,
  `"Đánh giá chất lượng tổng thể","${testSummary.overallQuality}"`,
  `"Khuyến nghị","${testSummary.recommendation}"`,
].join('\r\n');
csvFiles['00_Test_Summary'] = summaryCsv;

// Write all CSV files
import { mkdirSync } from 'fs';
mkdirSync('./test-report', { recursive: true });

for (const [name, content] of Object.entries(csvFiles)) {
  const filePath = `./test-report/${name}.csv`;
  writeFileSync(filePath, '\uFEFF' + content, 'utf8'); // BOM for Excel
  console.log(`✅ Created: ${filePath}`);
}

console.log('\n📊 Test Summary:');
console.log(`   Total Test Cases: ${testSummary.totalTestCases}`);
console.log(`   PASS: ${testSummary.passed}`);
console.log(`   FAIL: ${testSummary.failed}`);
console.log(`   Pending: ${testSummary.pending}`);
console.log(`   Pass Rate: ${testSummary.passRate}`);
console.log(`   Bugs Found: ${bugsFound.length} (${testSummary.criticalBugs} cao, ${testSummary.mediumBugs} trung bình, ${testSummary.lowBugs} thấp)`);
console.log('\n📁 CSV files have been created in ./test-report/');
console.log('   Open them in Excel or use the Excel generator below.');
console.log('\n💡 To create a single XLSX file, run:');
console.log('   node generate-excel-xlsx.mjs');
