# UI Consistency Rules

- All colors must come from CSS tokens.
- Spacing must follow the 4px scale.
- Every component must support dark mode.
- Respect the defined radius system: rounded-sm, rounded-md, rounded-lg, etc.
- No mixed icon libraries; ONLY Lucide React.
