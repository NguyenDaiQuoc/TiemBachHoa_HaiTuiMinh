Phase 6D: Interaction Polish & API Stabilization - COMPLETED

1. Account Dropdown:
   - Added hover-to-open support for desktop.
   - Added 150ms close delay for better mouse accessibility.
   - Implemented AnimatePresence and motion for smooth transitions.
   - Fixed z-index layering conflicts with Header.

2. Auth Flow:
   - Fixed keyboard tab order in LoginPage (Email -> Password).
   - Hidden password recovery from primary tab flow (tabIndex={-1}).

3. API Stability:
   - Added try/catch and mock fallbacks to auth login and product list.
   - Preview now survives Prisma/DB failures gracefully.

4. Dark Mode:
   - Hardened dark mode tokens in index.css.
   - Deepened background and card surfaces.
   - Improved WCAG contrast for muted text.

5. General interaction polish.
